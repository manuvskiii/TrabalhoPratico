<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TrabalhoPratico"
        tools:targetApi="31"
        android:usesCleartextTraffic="true">

        <!-- key:AIzaSyAL3Vocb1fmXk_mSNq87IaY_9euG4SnIZI Google Maps-->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAL3Vocb1fmXk_mSNq87IaY_9euG4SnIZI" />

        <!-- MainActivity(atividade principal) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- SecondActivity -->
        <activity
            android:name=".EscolasActivity"
        />
        <!-- Third Activity -->
        <activity android:name=".CursosActivity" />

        <!-- Fourth Activity -->
        <activity android:name=".EstagiosActivity" />

        <!-- Fivth Activity -->
        <activity android:name=".MapsActivity" />

        <!-- Sixth Activity -->
        <activity android:name=".LocaisActivity" />

    </application>

</manifest>
